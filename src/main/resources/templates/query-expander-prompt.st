Rewrite the user's question into {number} diverse, SHORT paraphrases for document retrieval.
RULES:
- Output EXACTLY {number} lines.
- One paraphrase per line.
- No numbering/bullets, no blank lines, no extra text.
- Keep quoted terms and product/org names unchanged.
- ≤ 12 words each.
User query: {query}